import { css } from 'styled-components';

const katexStyle = css`
    .katex {
        .mathnormal {
            font-size: 1.2rem;
        }
        .msupsub .mord {
            font-size: 0.8rem;
        }
    }
`;

export default katexStyle;