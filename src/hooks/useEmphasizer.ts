import { useEffect } from "react";

export default function useEmphasizer() {
    const currentY = globalThis.scrollY;
    const headers = document.querySelectorAll(`article > h2, h3, h4`)
    useEffect(() => {

    }, [])
}